# References

`Notion_2013.pdf` is the user-provided source document for visual and product direction. It is not loaded by the production application.

Temporary page renders and extracted working images belong in `references/extracted/` and are ignored by Git.
