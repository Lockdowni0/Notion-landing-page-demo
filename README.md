# Notion 2013 Landing Page

An interactive landing page based on the March 2013 Notion vision: “We shape our tools, and thereafter our tools will shape us.”

## Commands

```bash
npm install
npm run dev
npm test
npm run build
```

Project specifications live in `docs/`. The source PDF is stored in `references/` and is not a runtime dependency.
